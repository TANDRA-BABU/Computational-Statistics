# Polynomial Regression (Degree 2)
import numpy as np

x = np.array([1, 2, 3, 4, 5], dtype=float)
y = np.array([2, 4, 5, 4, 5], dtype=float)

coeffs = np.polyfit(x, y, 2)

print("Quadratic Equation:")
print(f"y = {coeffs[0]:.4f}x² + {coeffs[1]:.4f}x + {coeffs[2]:.4f}")
